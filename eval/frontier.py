"""Efficiency frontier (Phase 6b) -- the signature CE artifact. STUB.

Sweeps the context-token budget (cap retrieved+compressed context at 250, 500, 1k, 2k,
"dump-all") and plots accuracy (danger_recall & citation_faithfulness) vs avg tokens, showing
the engineered pipeline reaches near-max accuracy far to the LEFT (cheaper) of naive RAG.

Built out in Phase 6. Runnable now so the layout is complete.
"""
from __future__ import annotations

print("frontier.py: to be implemented in Phase 6 (sweep token_budget over the FULL config, save PNG).")
