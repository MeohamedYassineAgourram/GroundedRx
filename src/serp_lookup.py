"""Optional cached live contraindication lookup (Appendix C). STUB -- DO NOT BUILD YET.

Only touch this once Section 13's checklist is green. Reads/writes data/serp_cache.json;
never calls live during the pitch. Strengthens the "context engineering over *fresh* data"
angle by distilling a cached live lookup into grounded chunks.
"""
from __future__ import annotations

print("serp_lookup.py: OPTIONAL (Appendix C). Not built until Section 13 checklist is green.")
